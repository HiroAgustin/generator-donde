{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {
    "express": ">=4.4.0",
    "compression": ">=1.0.0",
    "body-parser": ">=1.3.0"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
