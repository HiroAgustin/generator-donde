language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-donde' ]; then cd .. && eval "mv $currentfolder generator-donde" && cd generator-donde; fi

